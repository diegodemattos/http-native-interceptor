import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'http-native-interceptor',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
